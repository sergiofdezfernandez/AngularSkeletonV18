echo '🏗️👷 Styling, testing and building your project before committing'

# Check Prettier standards
npm run format ||
(
    echo 'Prettier Format Failed 😭 Make the required changes listed above, add changes and try to commit again.'
    false;
)

# Check ESLint Standards
npm run lint ||
(
        echo 'ESLint Check Failed. Make the required changes listed above, add changes and try to commit again.'
        false; 
)

# Check ESLint Standards
npm run test ||
(
        echo 'Unit Tests Failed. Make the required changes listed above, add changes and try to commit again.'
        false; 
)

echo 'Code looks good to me... Trying to build now.'

npm run build ||
(
    echo '❌👷🔨❌ Better call Bob... Because your build failed ❌👷🔨❌
            Next build failed: View the errors above to see why. 
    '
    false;
)

# If everything passes... Now we can commit
echo '✅✅✅✅ You win this time... I am committing this now. ✅✅✅✅'